spring:
  datasource:
    url: jdbc:mariadb://172.17.0.1:3306/Eskimo?useUnicode=true&characterEncoding=utf8
    username: eskimo
    password: Eskimo2020Dev!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    properties:
      hibernate:
        show-sql: false
        format_sql: true
    hibernate:
      ddl-auto: create

  jackson:
    deserialization:
      fail-on-unknown-properties : true

  servlet:
    multipart:
      enabled: true
      max-file-size: 200MB
      max-request-size: 215MB

  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

jwt:
  secretKey: govlepel@$&amp;


log:
  slack:
    enabled: true
    webHookUrl: https://hooks.slack.com/services/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
    channel: error-report-bot
  database:
    enabled: true
  level: ERROR


file:
  upload-dir: ./files
  projectimage-dir: ./files/projectimg
  profileimage-dir: ./files/profileimg


logging:
  level:
    root: info
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
